@model GlobeWork.Models.ApplyJob
@{
    ViewBag.Title = "Chi tiết CV ứng viên";
    Layout = "~/Views/Shared/_EmployerLayout.cshtml";
}

<div class="p-3">
    @if (Model.FileUpload != null)
    {
        <div class="mt-3">
            <iframe src="@Path.Combine("/images/chapter/", Model.FileUpload)" style="width: 100%;height: 100vh;border: none;"></iframe>
        </div>
    }

</div>