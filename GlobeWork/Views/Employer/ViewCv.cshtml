@model GlobeWork.Models.ApplyJob
@{
    ViewBag.Title = "Chi tiết CV ứng viên";
    Layout = "~/Views/Shared/_EmployerLayout.cshtml";
}

<div class="p-3">
    @if (Model.FileUpload != null)
    {
        <div class="mt-3">
            <iframe src="@Path.Combine("/images/cv/", Model.FileUpload)" style="width: 100%;height: 100vh;border: none;"></iframe>
            @*<embed src="@Path.Combine("/images/cv/", Model.FileUpload)" type="application/pdf" width="100%" height="600px" />*@
        </div>
    }
</div>